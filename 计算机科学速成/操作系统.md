# 诞生
计算机越来越快,甚至运行的时间比放置程序的时间还要短,于是操作系统应运而生

# 本质
1. 开机的第一个程序,其他所有程序由它启动(原本一次给一个程序,现在一次给多个---批处理)
2. 操作系统屏蔽硬件层面更多细节,向应用层提供统一的api,供程序员调用
3. 通过(通过中断实现)调度实现多任务
4. 操作系统隐藏物理内存(乱序)的实际位置,映射到一个虚拟内存(顺序),我们只需要使用这个虚拟内存即可

